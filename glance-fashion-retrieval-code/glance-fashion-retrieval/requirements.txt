# Tested against Python 3.12 in a clean environment.
# Pinned to versions verified to work together -- in particular, transformers
# renamed OwlViTProcessor's post-processing method across versions
# (post_process_object_detection -> post_process_grounded_object_detection),
# so don't casually bump this without re-checking indexer/localizer.py.
torch>=2.5
transformers==5.14.1
faiss-cpu==1.14.3
scikit-learn>=1.4
numpy>=1.26
Pillow>=10.0
tqdm>=4.66
